<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../partials/head') %>
    <title><%= title %></title>
</head>
<body>
<%- include('../partials/header') %>

<div class="main">
    <div class="main-content">
        <div class="container">
            <div class="box">
                <div class="box-body">
                    <% if (success) { %>
                        <div class="alert alert-success"><%= success %></div>
                    <% } %>
                    <% if (error) { %>
                        <div class="alert alert-danger"><%= error %></div>
                    <% } %>

                    <a href="/admin/project/list" class="btn btn-secondary mt-3">Voltar à Lista de Projetos</a>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="overlay"></div>
</body>
</html>
